import { JobStatus } from "@/types/types";
import React from "react";

const HistoryVideoCard = ({ id, state, progress }: JobStatus) => {
  return <div>HVideoCard</div>;
};

export default HistoryVideoCard;
